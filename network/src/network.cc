#include "include/network.hpp"

// Network::Network()
// {
//     inet_subscribe_ping(inet_ptr.get(), &consume_ping);
//     inet_subscribe_pong(inet_ptr.get(), &consume_pong);
//     inet_subscribe_query(inet_ptr.get(), &consume_query);
//     inet_subscribe_query_hit(inet_ptr.get(), &consume_query_hit);
// }

/*
1. Весь Workflow сети расписать
2. Написать имплементацию
3. Написать имплементацию TCP/IP
*/
